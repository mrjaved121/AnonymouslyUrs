// backend/routes/communityRoutes.js
const express = require("express");
const router = express.Router();

// Example placeholder route — you can expand it later
router.get("/", (req, res) => {
  res.json({
    message: "Community routes are working properly!",
  });
});

module.exports = router;
